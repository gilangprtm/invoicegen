# Anti-Slop Audit 001 — InvoiceGen (2026-09-12)

Mode: AFTER (audit only, no fix applied)
Scope: `/mnt/storage/projects/invoicegen` @ `main` `c091e34`, working tree clean
Skills: antislop core + antislop-ui + antislop-human + antislop-layoutmobile + antislop-copywriting
KITAB: HEALTHY (70 markdown, 63 dokumen, registry tidak stale) — `kitab status`
Design Read: dashboard operasional invoice untuk individu/bisnis kecil, data-first, dial ENERGY 1 / RHYTHM 2 / MOTION 1 (dari SPEC Cycle 3)

> Aturan pakai: Tuan pilih nomor yang diperbaiki, saya hanya fix nomor yang di-approve, lalu re-run Delivery Gate.

## PASS (pertahankan, jangan dirusak)

- R-17/R-18/R-36/R-38 PASS: tidak ada statistik palsu, testimoni palsu, klaim fabrikasi. Semua angka dari `useInvoiceStore` (`src/routes/(main)/dashboard/index.tsx`, `src/stores/invoice-store.ts`).
- R-27 PASS: empty + loading (`Spinner`) + error (`Local storage unavailable`) ada di `dashboard/index.tsx`.
- R-24 PASS: sidebar hanya 4 tujuan nyata (`/dashboard`, `/dashboard/invoice`, `/dashboard/clients`, `/dashboard/settings`) di `src/navigation/sidebar/sidebar-items.ts`.
- R-15 PASS: CTA spesifik (`Create Invoice`, `Add Client`), bukan generik.
- R-28 PASS: tidak ada FAQ generik.

## Findings (numbered, priority = rule tier)

### HIGH (Hard Gate)

**001 — R-37 Design Direction Required — tidak ada DESIGN.md.** Alasan: UI tanpa arah adalah draft, bukan deliverable, wajib dial jujur ENERGY 1 / RHYTHM 1 / MOTION 1.
Evidence: tidak ada `DESIGN.md` di repo root; `AGENTS.md` tanpa pointer arah visual; evaluasi DRAFT-SK-2026-002 mencatat kondisi ini.
Rancangan fix: buat `DESIGN.md` via skill `design-md` (identitas InvoiceGen data-first, dial ENERGY 1 / RHYTHM 2 / MOTION 1 sesuai SPEC Cycle 3, tulis Design Read 1 baris).

**002 — R-31 Every Decision Must Have a Reason — tidak ada alasan tertulis untuk keputusan visual utama.** Alasan: teknik tanpa alasan tertulis adalah default, bukan keputusan.
Evidence: tidak ada baris alasan untuk palette `primary`, grid 4 `SummaryCard`, font `geist`, radius — cek `src/routes/(main)/dashboard/index.tsx`, `src/lib/preferences/`.
Rancangan fix: tulis 1 baris per keputusan di `DESIGN.md` (warna, layout, tipografi, spacing, cards).

**003 — R-38 Real Content or Honest Placeholder — placeholder search meniru template.** Alasan: copy generik menyamar sebagai final, bukan placeholder jujur.
Evidence: `src/routes/(main)/dashboard/-components/sidebar/search-dialog.tsx:146` berisi `Search dashboards, users, and more…` padahal hanya ada invoice/clients/settings.
Rancangan fix: ganti menjadi `Search invoices, clients…`.

**004 — R-25 Color Contrast + C-4 Resilience — badge overdue tanpa gaya.** Alasan: status yang hanya mengandalkan teks tanpa sinyal visual gagal di semua state.
Evidence: `src/routes/(main)/dashboard/invoice/index.tsx:24-29` `overdue: ""` vs dashboard `bg-red-100 text-red-700` — inkonsisten, kontras belum terverifikasi.
Rancangan fix: samakan dengan dashboard + verifikasi kontras 4.5:1 via `contrast-check.py`.

### MEDIUM (Purpose-Gate)

**005 — R-04 Icons — Lucide sebagai default tanpa alasan tertulis.** Alasan: glyph relevan tapi library look seragam tanpa keputusan tertulis.
Evidence: `LayoutDashboard, ReceiptText, Users, Settings` di `sidebar-items.ts`; `Eye, Pencil, Plus, Trash2` di `invoice/index.tsx` — tidak ada alasan pemilihan set.
Rancangan fix: 1 baris di DESIGN.md (Lucide = konsisten shadcn base-nova, stroke tipis untuk data-first), lalu PASS.

**006 — R-14 Feature Cards — 4 SummaryCard identik tanpa hierarki tertulis.** Alasan: kartu seragam meratakan bobot konten.
Evidence: `dashboard/index.tsx` `SummaryCard` x4 (Total, Unpaid, Paid, Draft) size/padding/layout identik.
Rancangan fix: nyatakan hierarchy flat disengaja ATAU beri variasi kecil + tulis alasan.

**007 — R-05 Layout — rhythm monoton + default dashboard shell.** Alasan: sidebar + stat row + chart + table adalah default memori, bukan dari keputusan screen.
Evidence: `dashboard/index.tsx` (grid 4 kartu, revenue + status, recent 2 kolom) — section composition seragam, RHYTHM 2 diklaim tapi belum bervariasi.
Rancangan fix: tulis job screen (lihat tagihan macet + aksi cepat) + benarkan susunan, atau variasikan 1 section.

**008 — R-06 Typography — font tanpa alasan brand.** Alasan: typeface default model tanpa alasan adalah tell.
Evidence: `preferences-config.ts` default `geist`, tidak ada alasan brand-character.
Rancangan fix: 1 baris alasan di DESIGN.md.

### LOW (Quality Locks / Craftsmanship)

**009 — Sterile Default + R-29 Palette — tanpa accent yang disengaja.** Alasan: void bersih tanpa focal point bukan desain selesai.
Evidence: dashboard flat white/grey, thin border, `primary` tersebar tanpa focal point tunggal.
Rancangan fix: tetapkan one deliberate accent di DESIGN.md (1 focal point per screen).

**010 — R-32 Keyboard + R-26 Interactive — belum ada bukti click-through.** Alasan: klaim PASS tanpa daftar klik terekam bukan PASS.
Evidence: tidak ada log `R-35` click-through (setiap tombol, dialog Escape, toggle theme, breakpoint mobile).
Rancangan fix: jalankan dan catat click-through element-by-element setelah Fase 0 baseline (`npm install` + `build`).

**011 — R-34 Every Theme Must Work — toggle ada tapi bukti dua mode belum ada.** Alasan: theme yang dikirim harus dua-duanya berfungsi.
Evidence: `layout-controls.tsx` + `theme-switcher.tsx` ada, badge `dark:` class ada, tapi belum ada verifikasi light/dark + kontras.
Rancangan fix: cek kedua mode + fokus indikator 3:1 di tiap theme.

**012 — R-03 Mobile — reflow belum terverifikasi di rentang penuh.** Alasan: dua state saja (phone stack vs desktop grid) meninggalkan tablet sebagai kebetulan.
Evidence: grid `sm:grid-cols-2 xl:grid-cols-4`, `lg:grid-cols-[1.3fr_1fr]` — belum ada bukti drag viewport 360-1280 tanpa overflow + tap target 44px.
Rancangan fix: verifikasi tiap breakpoint + catat hasil (bagian Delivery Gate C1).

## Catatan stok UI (keputusan Tuan, bukan slop)

Keep seluruh `src/components/ui` (60 file) sebagai ready-to-use inventory disengaja. Bukan slop karena technique with purpose bila dinyatakan eksplisit di `AGENTS.md`. Prune hanya deps backend zero-import (`better-auth`, `drizzle-orm`, `postgres`, dsb) + `.env.example`/`drizzle.config.ts` yang menipu — selektif per `grep from`, karena `dnd-kit` kemungkinan dipakai reorder item (`aria-label Reorder` di `invoice-items.tsx`).

## Next (tunggu approval Tuan)

1. Tuan pilih nomor (misal 001-004 dulu).
2. Saya fix hanya nomor terpilih + re-run Delivery Gate 4 blok + bukti build/klik/kontras/viewport.
3. Nomor lain tidak disentuh.
